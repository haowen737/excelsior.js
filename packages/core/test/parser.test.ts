import xlsx from 'xlsx'
import { Excelsior, ExcelsiorConfig, Builder } from '../src'

// const Data = [['name', 'age'], ['Amy', 12], ['Sam', 13]]
// const result = Builder.build(Data)
// console.log('result---', result)

describe('reader', () => {

  it('can read excel', (done) => {
    done()
  })

  it('can read buffer', (done) => {
    done()
  })

  it('can read multiple sheets', (done) => {
    done()
  })

  it('can split excel', (done) => {
    done()
  })

  it('can run excel in worker thread', (done) => {
    done()
  })

  it('can throw error when read failed', (done) => {
    done()
  })
  
})